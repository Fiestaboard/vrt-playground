{
  "name": "vrt-playground",
  "version": "1.0.0",
  "private": true,
  "description": "Playground for trying fiestaboard/visual-regression-action — a fake dashboard, Playwright capture, and the full review/approve flow",
  "type": "module",
  "scripts": {
    "capture": "node scripts/capture.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
